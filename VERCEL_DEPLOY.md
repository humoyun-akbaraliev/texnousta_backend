# 🚀 Деплой на Vercel

## ⚠️ Ограничения Vercel для Go:
- Только serverless functions
- Нет постоянного соединения с БД
- Холодные старты
- Ограничения по времени выполнения

## 📁 Созданные файлы для Vercel:
- `vercel.json` - конфигурация
- `api/index.go` - serverless handler
- `vercel-build.js` - настройки сборки

## 🔧 Команды для деплоя:

### 1. Установить Vercel CLI:
```bash
npm install -g vercel
```

### 2. Логин в Vercel:
```bash
vercel login
```

### 3. Деплой:
```bash
vercel --prod
```

## ⚙️ Переменные окружения в Vercel:
Добавьте в Vercel dashboard:
- `GIN_MODE=release`
- `DATABASE_URL=postgresql://...` (ваш Neon URL)

## 🔗 После деплоя:
- API: `https://your-project.vercel.app/api/v1/`
- Тест: `https://your-project.vercel.app/api/v1/categories`

## ⚡ Альтернативы (если Vercel не подойдет):
1. **Render** (бесплатный план)
2. **Fly.io** (бесплатный план)
3. **Google Cloud Run** (бесплатный лимит)
4. **DigitalOcean Apps** ($5/месяц)