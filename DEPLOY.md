# 🚀 Деплой TexnoUsta API

Go приложения **нельзя деплоить на Vercel**, но есть отличные альтернативы!

## 📋 Быстрые варианты деплоя

### 1. 🚂 **Railway** (Рекомендуется - самый простой)

#### Шаг 1: Подготовка
```bash
# Установить Railway CLI
npm install -g @railway/cli

# Убедиться что проект в Git
git add .
git commit -m "Подготовка к деплою"
```

#### Шаг 2: Деплой
```bash
# Логин в Railway
railway login

# Инициализация проекта
railway init

# Деплой
railway up
```

#### Шаг 3: Настройка переменных
В Railway dashboard добавьте переменные:
- `GIN_MODE=release`
- `JWT_SECRET=your-super-secret-jwt-key-here`
- `PORT=8080` (автоматически)

**✅ Готово! API будет доступен по ссылке Railway**

---

### 2. 🎨 **Render** (Есть бесплатный план)

#### Шаг 1: Создать аккаунт на render.com

#### Шаг 2: Подключить GitHub репозиторий

#### Шаг 3: Настройки деплоя
- **Build Command:** `go build -o main cmd/main.go`
- **Start Command:** `./main`
- **Environment:** `Go`

#### Шаг 4: Переменные окружения
- `GIN_MODE=release`
- `JWT_SECRET=your-secret-key`

---

### 3. 🌊 **DigitalOcean App Platform**

#### Шаг 1: Создать приложение
- Подключить GitHub репозиторий
- Выбрать Go как язык

#### Шаг 2: Настройки
- **Build Command:** `go build cmd/main.go`
- **Run Command:** `./main`

---

### 4. ☁️ **Google Cloud Run** (Serverless)

#### Dockerfile уже готов! Просто:
```bash
# Установить gcloud CLI
# Логин
gcloud auth login

# Деплой
gcloud run deploy texnousta-api \
  --source . \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated
```

---

## 🔧 Что уже настроено для деплоя

### ✅ Файлы готовы:
- `Procfile` - для Heroku-compatible платформ
- `Dockerfile` - для контейнерного деплоя  
- `railway.toml` - для Railway
- `render.yaml` - для Render
- `.env.example` - пример переменных окружения

### ✅ Код готов:
- Поддержка переменной `PORT` из окружения
- SQLite база (портативная)
- CORS настроен для продакшена
- Swagger документация

---

## 🌟 Рекомендую Railway - вот почему:

### ✅ **Преимущества Railway:**
- 🚀 **Деплой в 3 команды** (login, init, up)
- 💰 **Щедрый бесплатный план** ($5 в месяц кредитов)
- 🔄 **Автодеплой** при push в Git
- 📊 **Удобный dashboard** с логами и метриками
- 🗄️ **Встроенные базы данных** (PostgreSQL, MySQL, Redis)
- 🌍 **Глобальные CDN**

### 📱 **После деплоя получите:**
- `https://your-app.railway.app` - основной API
- `https://your-app.railway.app/swagger/index.html` - Swagger UI
- Автоматический HTTPS сертификат
- Мониторинг и логи

---

## ⚙️ Переменные окружения для продакшена

Установите эти переменные в выбранной платформе:

```env
# Обязательные
GIN_MODE=release
JWT_SECRET=ваш-очень-секретный-ключ-длиной-минимум-32-символа
PORT=8080

# Опциональные
DATABASE_URL=sqlite://./texnousta.db (по умолчанию)
```

---

## 🔄 Миграция на PostgreSQL (для продакшена)

Если хотите использовать PostgreSQL вместо SQLite:

### 1. Добавить драйвер PostgreSQL:
```bash
go get gorm.io/driver/postgres
```

### 2. Обновить подключение к БД:
```go
// В internal/database/database.go
import "gorm.io/driver/postgres"

// Заменить SQLite на PostgreSQL
dsn := os.Getenv("DATABASE_URL") // получите из Railway/Render
db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{})
```

### 3. На Railway/Render добавить PostgreSQL:
- Railway: `railway add postgresql`
- Render: Add PostgreSQL service

---

## 🧪 Тестирование после деплоя

После деплоя протестируйте:

```bash
# Замените на ваш URL
API_URL="https://your-app.railway.app"

# Тест здоровья API
curl "$API_URL/api/v1/products"

# Тест Swagger
curl "$API_URL/swagger/index.html"

# Тест контактной формы (только телефон)
curl -X POST "$API_URL/api/v1/phone-contact" \
  -H "Content-Type: application/json" \
  -d '{"phone": "+998901234567"}'
```

---

## 🎯 Пошаговый деплой на Railway (самый простой):

### 1. **Подготовка (1 минута)**
```bash
# Убедитесь что код в Git
git add .
git commit -m "Ready for deployment"

# Установить Railway CLI
npm install -g @railway/cli
```

### 2. **Деплой (2 минуты)**
```bash
# Логин
railway login

# Создать проект
railway init

# Деплой!
railway up
```

### 3. **Настройка (1 минута)**
В Railway dashboard:
- Откройте Variables
- Добавьте: `GIN_MODE=release`
- Добавьте: `JWT_SECRET=your-secret-key-here`
- Сохранить и подождать редеплоя

### 4. **Готово! 🎉**
- Ваш API: `https://your-project-name.railway.app`
- Swagger: `https://your-project-name.railway.app/swagger/index.html`

---

## 💡 Полезные команды Railway:

```bash
# Посмотреть логи
railway logs

# Открыть в браузере
railway open

# Локальная разработка с переменными Railway
railway shell

# Добавить PostgreSQL
railway add postgresql

# Статус деплоя
railway status
```

---

## 🔗 Обновление фронтенда

После деплоя обновите базовый URL в React:

```javascript
// В src/services/api.js
const API_BASE_URL = 'https://your-app.railway.app/api/v1';
```

---

**🚀 Готово к деплою! Выберите платформу и следуйте инструкциям выше.**

**Рекомендую Railway - самый быстрый способ задеплоить Go API!**