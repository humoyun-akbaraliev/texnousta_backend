# 📚 Swagger API Documentation

## 🚀 Быстрый доступ

**Swagger UI доступен по адресу:** [http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)

## 📖 Что такое Swagger?

Swagger (OpenAPI) - это автоматически генерируемая интерактивная документация API, которая позволяет:

- ✅ **Просматривать** все доступные эндпоинты
- ✅ **Тестировать** API прямо в браузере 
- ✅ **Изучать** структуру запросов и ответов
- ✅ **Авторизовываться** и работать с защищенными эндпоинтами
- ✅ **Экспортировать** документацию в различных форматах

## 🏷️ Группы эндпоинтов

### 🔐 **auth** - Авторизация
- `POST /register` - Регистрация нового пользователя
- `POST /login` - Авторизация с получением JWT токена
- `GET /profile` - Получить профиль пользователя 🔒
- `PUT /profile` - Обновить профиль пользователя 🔒

### 📱 **products** - Товары
- `GET /products` - Список товаров с фильтрацией и пагинацией
- `GET /products/{id}` - Получить конкретный товар

### 🏷️ **categories** - Категории
- `GET /categories` - Список всех активных категорий

### 📞 **contact** - Обратная связь
- `POST /contact` - Отправить полную контактную форму
- `POST /quick-contact` - Быстрая заявка (имя + телефон)

### 🔒 **admin** - Админская панель
- **Товары:**
  - `POST /admin/products` - Создать товар 🔑
  - `PUT /admin/products/{id}` - Обновить товар 🔑
  - `DELETE /admin/products/{id}` - Удалить товар 🔑

- **Категории:**
  - `POST /admin/categories` - Создать категорию 🔑
  - `PUT /admin/categories/{id}` - Обновить категорию 🔑
  - `DELETE /admin/categories/{id}` - Удалить категорию 🔑

- **Обращения клиентов:**
  - `GET /admin/contacts` - Список обращений 🔑
  - `GET /admin/contacts/{id}` - Получить обращение 🔑
  - `PUT /admin/contacts/{id}/read` - Пометить как прочитанное 🔑
  - `DELETE /admin/contacts/{id}` - Удалить обращение 🔑

- **Пользователи:**
  - `GET /admin/users` - Список пользователей 🔑
  - `PUT /admin/users/{id}` - Обновить пользователя 🔑
  - `DELETE /admin/users/{id}` - Удалить пользователя 🔑

**Условные обозначения:**
- 🔒 - Требует авторизации (JWT токен)
- 🔑 - Требует роль администратора

## 🛠️ Как использовать Swagger UI

### 1. **Открытие Swagger UI**
Перейдите по ссылке: [http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)

### 2. **Просмотр эндпоинтов**
- Эндпоинты сгруппированы по тегам (auth, products, categories, contact, admin)
- Нажмите на группу для раскрытия списка эндпоинтов
- Нажмите на конкретный эндпоинт для просмотра деталей

### 3. **Тестирование эндпоинтов**
- Нажмите кнопку **"Try it out"**
- Заполните требуемые параметры
- Нажмите **"Execute"** для выполнения запроса
- Просмотрите ответ в разделе "Server response"

### 4. **Авторизация для защищенных эндпоинтов**

#### Шаг 1: Получите JWT токен
1. Перейдите к эндпоинту `POST /login`
2. Нажмите **"Try it out"**
3. Введите данные админа:
   ```json
   {
     "email": "admin@texnousta.com",
     "password": "password"
   }
   ```
4. Нажмите **"Execute"**
5. Скопируйте значение `token` из ответа

#### Шаг 2: Авторизация в Swagger
1. Нажмите кнопку **"Authorize"** в правом верхнем углу
2. В поле "Value" введите: `Bearer ВАШ_JWT_ТОКЕН`
   ```
   Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJlbWFpbCI...
   ```
3. Нажмите **"Authorize"**
4. Теперь вы можете тестировать защищенные эндпоинты!

#### Шаг 3: Тестирование админских функций
После авторизации можете тестировать админские эндпоинты:
- Просмотр обращений клиентов
- Создание/редактирование товаров и категорий
- Управление пользователями

## 📝 Примеры запросов

### Публичные эндпоинты

#### Получить товары с фильтром:
```
GET /products?category=1&featured=true&limit=5
```

#### Отправить быструю заявку:
```json
POST /quick-contact
{
  "name": "Иван Петров",
  "phone": "+998901234567"
}
```

#### Полная контактная форма:
```json
POST /contact
{
  "name": "Мария Иванова",
  "email": "maria@example.com",
  "phone": "+998901234568",
  "subject": "Консультация",
  "message": "Хочу узнать о наличии iPhone 15 Pro"
}
```

### Защищенные эндпоинты

#### Создать товар (админ):
```json
POST /admin/products
{
  "name": "iPhone 16 Pro",
  "description": "Новейший iPhone с улучшенными характеристиками",
  "price": 1500,
  "old_price": 1600,
  "category_id": 1,
  "brand": "Apple",
  "model": "iPhone 16 Pro",
  "stock": 20,
  "is_active": true,
  "is_featured": true
}
```

#### Создать категорию (админ):
```json
POST /admin/categories
{
  "name": "Планшеты",
  "description": "iPad и Android планшеты",
  "is_active": true
}
```

## 🔧 Полезные возможности Swagger UI

### 1. **Модели данных**
- Внизу страницы находится раздел "Models"
- Здесь можно посмотреть структуру всех объектов API
- Полезно для понимания формата данных

### 2. **Коды ответов**
- Каждый эндпоинт показывает возможные коды ответов
- 200/201 - успешно
- 400 - ошибка в запросе
- 401 - не авторизован
- 403 - доступ запрещен
- 404 - не найдено
- 500 - ошибка сервера

### 3. **Экспорт документации**
- Можно скачать OpenAPI спецификацию в JSON/YAML формате
- Использовать для генерации клиентского кода
- Импорт в Postman или Insomnia

## 🚀 Преимущества Swagger для фронтенда

### ✅ **Для разработчиков:**
- Не нужно изучать отдельную документацию
- Интерактивное тестирование прямо в браузере
- Актуальная документация (генерируется из кода)
- Понятные примеры запросов и ответов

### ✅ **Для тестирования:**
- Быстрое тестирование всех сценариев
- Проверка авторизации
- Валидация данных
- Отладка API перед интеграцией

### ✅ **Для интеграции:**
- Точные модели данных
- Список всех доступных параметров
- Примеры успешных и ошибочных ответов
- Информация о требуемой авторизации

## 🔄 Обновление документации

Когда вы добавляете новые эндпоинты или изменяете существующие:

1. **Добавьте Swagger аннотации** к функциям-обработчикам:
   ```go
   // CreateProduct создает новый товар
   //
   //	@Summary		Создать товар
   //	@Description	Создание нового товара
   //	@Tags			admin
   //	@Accept			json
   //	@Produce		json
   //	@Security		BearerAuth
   //	@Param			product	body		models.ProductRequest	true	"Данные товара"
   //	@Success		201		{object}	map[string]interface{}
   //	@Router			/admin/products [post]
   func CreateProduct(c *gin.Context) {
   ```

2. **Обновите документацию:**
   ```bash
   export PATH=$PATH:$(go env GOPATH)/bin
   swag init -g cmd/main.go
   ```

3. **Перезапустите сервер** и документация обновится автоматически!

## 🎉 Готово к использованию!

Теперь у вас есть полная интерактивная документация API! 

**Ссылка для фронтенд команды:** [http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)

**Данные для админского доступа:**
- Email: `admin@texnousta.com`
- Password: `password`

Ваш фронтенд-разработчик может теперь:
- 🔍 Изучить все доступные эндпоинты
- 🧪 Протестировать их в интерактивном режиме  
- 📋 Понять структуру данных и требования
- 🔐 Проверить авторизацию и админские функции