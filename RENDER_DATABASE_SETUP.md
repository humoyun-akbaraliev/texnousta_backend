# 🚨 ВАЖНО: Настройка переменных окружения на Render

## Проблема
Данные сохраняются, но исчезают после перезапуска сервера. Это происходит потому, что используется SQLite (файловая БД) вместо PostgreSQL.

## Решение
Убедитесь, что на Render установлены все переменные окружения:

### 1. Зайдите в Render Dashboard
- Перейдите к вашему сервису TexnoUsta_Backend
- Откройте вкладку "Environment"

### 2. Добавьте следующие переменные:

```
GIN_MODE=release
PORT=8080  
JWT_SECRET=your-super-secret-jwt-key-here-change-this-to-secure-key-32-chars-min
DATABASE_URL=postgresql://neondb_owner:npg_UfiA4BeTNC3P@ep-flat-bonus-agmh7719-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

### 3. Особенно важно:
**DATABASE_URL** - это самая важная переменная! Без неё сервер будет использовать SQLite, который очищается при каждом деплое.

### 4. После добавления переменных:
- Нажмите "Save Changes"  
- Render автоматически перезапустит сервис
- В логах вы должны увидеть: `✅ Успешное подключение к базе данных PostgreSQL`

### 5. Проверка:
После деплоя попробуйте:
1. Добавить телефонный номер
2. Перезапустить сервис в Render (Manual Deploy)
3. Проверить, что номер остался в базе

## ⚠️ Если проблема сохраняется:
Проверьте логи деплоя в Render - там должно быть сообщение о подключении к PostgreSQL, а не SQLite.