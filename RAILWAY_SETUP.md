# 🚂 Деплой на Railway

## 📋 Быстрый деплой (3 команды):

### 1. Установить Railway CLI:
```bash
npm install -g @railway/cli
```

### 2. Авторизация:
```bash
railway login
```

### 3. Инициализация и деплой:
```bash
railway init
railway up
```

## 🗄️ Добавить PostgreSQL базу данных:
```bash
railway add postgresql
```

## ⚙️ Переменные окружения (автоматически):
Railway автоматически установит:
- `DATABASE_URL` (из PostgreSQL плагина)
- `PORT` (автоматически)

Вручную добавьте в Railway dashboard:
- `GIN_MODE=release`
- `JWT_SECRET=your-super-secret-jwt-key-here`

## 🎯 После деплоя получите:
- 🌐 **API**: `https://texnousta-backend-production.up.railway.app`
- 📚 **Swagger**: `https://texnousta-backend-production.up.railway.app/swagger/index.html`
- 📱 **Тест API**: `https://texnousta-backend-production.up.railway.app/api/v1/categories`

## 🔧 Полезные команды Railway:
```bash
# Посмотреть логи
railway logs

# Открыть проект в браузере
railway open

# Статус проекта
railway status

# Переменные окружения
railway variables

# Подключиться к базе данных
railway connect postgresql
```

## ✅ Готовые файлы для Railway:
- `railway.toml` - конфигурация Railway
- `Dockerfile` - для контейнеризации
- `Procfile` - команда запуска
- `go.mod` - зависимости Go
- `.env` - переменные окружения

## 🚀 Автодеплой:
После первого деплоя Railway автоматически будет деплоить при каждом push в main ветку!

## 💰 Лимиты Railway:
- **Бесплатный план**: $5 кредитов в месяц
- **Хватит на**: ~750 часов работы небольшого API
- **Upgrade**: $5/месяц за дополнительные ресурсы